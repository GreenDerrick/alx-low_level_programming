#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: 0 (Always)
 */
int main(void)
{
	char name[150];

	name = "Programming is like building a multilingual puzzle";
	gets(name);
	puts(name);
	return (0);
}
