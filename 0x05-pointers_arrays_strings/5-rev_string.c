#include "main.h"
/**
 * rev_string - reverse the string given
 *
 * @s: variable to store address
 */
void rev_string(char *s)
{
	int number;

	for (number = s['\0']; number != 0; number--)
	{
	}
	_putchar(s[number]);
	_putchar('\n');
}
