#include "main.h"
/**
 * mul - this is a short description
 * a - first  number
 * b - second number
 * Return: 0 Always
 */
int mul(int a, int b)
{
	int product;

	product = a * b;

	return (product);
}
