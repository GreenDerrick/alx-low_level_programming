#include <stdio.h>
#include <stddef.h>
#include <stdlib.h>
#include "variadic_functions.h"
#include <stdarg.h>
/**
 * print_all - function to print all types
 * @format: variable to be manipulated
 * Return: nothing
 */
void print_all(const char * const format, ...)
{
	int i;
	va_list(all);
	
	va_start (list, format);
	
}
