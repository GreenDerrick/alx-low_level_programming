This is the file for variadic functions.
-----------------------------------------------------------------------------
variadic functions are used to process as many parameters as the programs inputs
format is va_list(name); va_start(name, [last_variabe in the function]), va_args(name, [expected_data_type]), va_end(name);

please note: a list is ended before the return value is encountered
